import numpy as np

def init_simulation():# che parametri ci vanno?
	"""
    	Create the 0-generation and initialize the structures that contains data
    	Parameters:
    	-----------------------------
	
   	-----------------------------
    	Returns
    	"""
	#bisogna creare la popolazione e tutti gli oggetti per contenere le informazioni
	population = np.zeros(2,100) #creo un'array 2x100, che rappresenta la popolazione, nella prima riga metto la strategia k,nella seconda le risorse
	# ora bisogna creare la strategia per ogni individuo
	for individual in population[0]
		individual = 

def generate_random_int(low_value, high_value, sample):# tengo la generazione randomica separate per il testing
	np.random.randint(low_value, high_value, sample, dtype=np.short)
		
	